import math

def sieve(n):
    we will sieve in two diff
    - one to generate the list of primes up to n
    - one to generate the list of integers relatively prime to n

    we start by creating a list of booleans, which will serve as our sieve for PRIMES. we initialize all values to TRUE (except for 0 and 1)

    as we iterate through the list, we mark the multiples of each prime as FALSE. this way, at the end of the process, the indices that are still TRUE will correspond to prime numbers.

   

